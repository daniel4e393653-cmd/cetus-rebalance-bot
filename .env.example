# ============================================
# Cetus Rebalance Bot Configuration
# ============================================

# Required: Network to connect to (mainnet or testnet)
NETWORK=mainnet

# Required: Private key of the wallet
# Supports multiple formats:
#   1. Bech32 (recommended): suiprivkey1... 
#   2. Base64: AIUPxQveY18... (33-byte with scheme flag)
#   3. Hex: 64 character hex string (with or without 0x prefix)
# WARNING: Never share or commit this key!
PRIVATE_KEY=your_private_key_here

# Optional: Multiple RPC URLs (comma-separated) for load balancing and failover
# The bot will distribute requests across these endpoints and automatically
# retry failed requests using different RPCs
# Examples:
# RPC_URLS=https://fullnode.mainnet.sui.io,https://sui-mainnet-rpc.allthatnode.com,https://mainnet.suiet.app
# RPC_URLS=https://fullnode.testnet.sui.io,https://sui-testnet-endpoint.blockvision.org
RPC_URLS=https://fullnode.mainnet.sui.io

# Deprecated: Single RPC URL (still supported for backwards compatibility)
# If RPC_URLS is set, this will be ignored
# RPC_URL=https://fullnode.mainnet.sui.io

# Optional: Check interval in seconds (default: 30)
CHECK_INTERVAL_SECONDS=30

# Optional: Slippage tolerance in percent (default: 0.5)
SLIPPAGE_PERCENT=0.5

# Optional: Enable/disable rebalancing (default: true)
# Set to false to run in monitoring mode only (no transactions)
REBALANCE_ENABLED=true

# Optional: Log level (debug, info, warn, error)
LOG_LEVEL=info
